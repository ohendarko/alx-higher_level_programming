#!/usr/bin/python3
""" Each module, class, and
method must contain docstring as comments."""


class Square:
    """ Each module, class, and
    method must contain docstring as comments."""
    def __init__(self, size):
        self.__size = size
